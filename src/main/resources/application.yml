spring:
  application:
    name: CONFIG-SERVER

  cloud:
    config:
      server:
        git:
          uri: https://github.com/mvaibhav06/microservice-config
          clone-on-start: true

server:
  port: 8085


